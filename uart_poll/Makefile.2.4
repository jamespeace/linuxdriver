INCLUDEDIR = /usr/src/linux-$(shell uname -r)/include
DEBFLAGS = -O2
CFLAGS = -D__KERNEL__ -DMODULE -Wall $(DEBFLAGS)
CFLAGS += -isystem $(INCLUDEDIR)
OBJS = uart_ring.o ringbuf.o
all: $(OBJS)

clean:
	rm -rf *.o
